# Copy this file to .env and fill in your actual values
# Your webhook secret from Kontent.ai (Environment settings > Webhooks > Your webhook > Settings > Secret)
WEBHOOK_SECRET=your-actual-webhook-secret-here

# Optional: Change the port the server runs on (default: 3000)
# PORT=8080
